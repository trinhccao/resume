<!DOCTYPE html>
<html lang="{{ data.lang }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ data.page_title }}</title>
</head>
<body>
  {% render 'sections/header', data: data %}
  <main id="main">{{ page_content }}</main>
</body>
</html>