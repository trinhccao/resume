<section class="section">
  <h2 class="heading-lv2">{{ section_data.title }}</h2>
  <p class="paragraph">{{ section_data.description }}</p>
  {% render 'snippets/cta-buttons', section_data: section_data %}
</section>